#include "include/converter.h"

int counter(char *argv[])
{
    int array_count;
    array_count = strlen(argv[1]);
    
    return (array_count);
}

/*
int converter(char *argv[])
{
    char *array;
    array = argv[1];

    return (array);
}
*/
